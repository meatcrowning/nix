# open-webui writes this secret into cwd if launched from here — never commit it
.webui_secret_key
__pycache__/

/docs/

# `nixos-rebuild build` / `nix build` drop their out-link here.
/result
/result-*
