# ly dies at startup if its lang file is missing from the config dir, and this
# file just needs to exist: an empty one parses to ly's compiled English
# defaults, which are the same strings Fedora's /etc/ly/lang/en.ini contains.
